export type TUserCart = {
  productId: string;
  uniqueId: string;
  quantity: number;
  size: string;
  name: string;
  price: number;
  image: string;
};


export type TSumAndQuantity = {
    sum: number 
    qty: number
}